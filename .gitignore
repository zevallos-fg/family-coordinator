# Dependencies
node_modules/

# Wrangler cache
.wrangler/
**/.wrangler/

# Claude Code session state (if it creates any)
.claude/

# OS cruft
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Secrets — never commit these
*.env
.env*
# Cloudflare Worker secrets for `wrangler dev`. NOT covered by .env* above —
# the filename starts with ".dev", so it needs its own rule.
.dev.vars
**/.dev.vars

# Next.js
/.next/
/out/
*.tsbuildinfo
next-env.d.ts

# Vercel
.vercel

# Supabase CLI metadata
supabase/.temp/
supabase/.branches/

/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/

# Playwright auth fixture (session cookies + test family ids)
tests/e2e/.auth/
