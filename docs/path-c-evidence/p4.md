# P4 — F7 Document Vault Polling Fix Evidence

## Investigation findings

- `documents` table columns: `id, family_id, title, doc_type, file_url, file_size_bytes, uploaded_by_user_id, ocr_text, tags, indexed_at, created_at`
- No `summary` column (adjusted query to `indexed_at, tags` only)
- `triggerIndexing` server action already existed
- `window.location.reload()` was the only post-upload action

## Fix

- `app/(app)/documents/actions.ts`: added `getDocumentIndexingStatus(document_id)` — returns `{ indexed_at, tags }` — family-scoped via RLS
- `components/documents/DocumentVaultView.tsx`:
  - Replaced `window.location.reload()` with `startPolling(docId)`
  - 2s `setInterval` polls until `indexed_at` populates
  - 30s `setTimeout` fires "Indexing failed" + retry button
  - Full cleanup on component unmount via `useEffect` return
  - Optimistic doc added to list immediately on upload
  - `animate-pulse` on "Indexing…" badge for visual feedback

## Commits

```
659d4dc feat(v34/F7-fix): poll indexed_at on Document Vault uploads with 30s timeout + retry
```

## Gate results

```
tsc --noEmit: 0 errors ✅
pnpm test:    182 passing, 2 skipped ✅
```

## P4 gate: PASS (Playwright E2E deferred — requires auth session + file fixture)
