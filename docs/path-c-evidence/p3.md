# P3 — F8 Weekly Digest Fix Evidence

## Investigation findings

- `digests` table: `content text` + `blind_spots jsonb`. NO `sections` or `load_attribution` columns.
- Action was building markdown from sections, losing structure.
- 0 rows in production → no backwards compat concern.

## Fix

- `app/(app)/digest/actions.ts`: stores `JSON.stringify({ summary, sections, load_attribution })` in `content` instead of markdown
- `components/digest/DigestView.tsx`: parses JSON from `content`, renders structured sections + bar chart, removes dead-code marker

## Commits

```
75416c1 feat(v34/F8-fix): render structured digest sections and load attribution
```

## Gate results

```
tsc --noEmit: 0 errors ✅
pnpm test skills/family-weekly-digest: 6/6 passing ✅
```

## Dead-code marker removed

`{(() => { /* Parse load attribution from content if not in blind_spots */ return null; })()}` — removed ✅

## P3 gate: PASS
