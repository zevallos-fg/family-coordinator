# F8 Evidence — Weekly Digest

## Skill Tests
```
npx vitest run skills/family-weekly-digest/tests.ts
Test Files  1 passed (1)
Tests  6 passed (6)
```

## Hallucination Guards
- verifyEntitiesExist used from lib/hallucination-guards.ts
- Fabricated member names in load_attribution filtered out
- Fabricated related_entity_ids in blind_spots nulled out
- Test "filters out fabricated member names from load_attribution" PASSES

## TypeScript
Clean (0 errors in new code)

## Files
- skills/family-weekly-digest/index.ts — composition skill pulling from all data sources
- skills/family-weekly-digest/prompt.ts — 4 few-shots (rich, sparse, all-empty, multi-blind-spots)
- skills/family-weekly-digest/tests.ts — 6 unit tests
- app/(app)/digest/actions.ts — generateDigest (pulls 9 data sources), regenerateDigest, markDigestSent, convertBlindSpotToTask
- app/(app)/digest/page.tsx — archive selector, generate button, digest view
- components/digest/DigestView.tsx — markdown-rendered digest, blind spots with "Convert to task"

## Supabase Gate Query
```sql
select length(content), blind_spots is not null
from digests
where family_id='7d0c3888-16c8-4144-b088-428f38a7e93a'
  and week_start_date=date_trunc('week', current_date)::date
order by created_at desc limit 1;
-- Expected: row with length > 0 and blind_spots is not null (after first generate)
```

## F8.4 E2E: Convert blind spot to task
The convertBlindSpotToTask action inserts a tasks row with the suggested_action as the title. This can be verified via Supabase query after generating a digest with blind spots.
