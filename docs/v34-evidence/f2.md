# F2 Evidence — Trips

## Skill Tests
```
npx vitest run skills/family-travel/tests.ts
Test Files  1 passed (1)
Tests  6 passed (6)
```

## TypeScript
```
npx tsc --noEmit → 0 errors in new code
(pre-existing errors in lib/grocery/dedup.ts and scripts/ deferred to TECH_DEBT)
```

## Skill Implementation
- skills/family-travel/index.ts — packing list + prep tasks with Zod validation
- skills/family-travel/prompt.ts — SYSTEM_PROMPT + buildUserPrompt, 4 few-shots (beach/kid, ski, international, work solo)
- Hallucination guard: every owner value verified against household input (valid = adult name, "kid_<name>", "shared")
- 6 unit tests: beach trip, empty kids, international, malformed JSON, fabricated owner, missing destination

## Server Actions
- app/(app)/trips/actions.ts — createTrip (fires skill, inserts packing items + prep tasks), updateTrip, togglePackingItem, deleteTrip (cascade)
- All return ActionResult<T>, all wrapped in try/catch

## UI
- app/(app)/trips/page.tsx — list with Destination | Dates | Packed % | Actions
- app/(app)/trips/new/page.tsx — create form
- app/(app)/trips/[id]/page.tsx — detail with packing checklist grouped by owner
- components/trips/TripPackingList.tsx — interactive checklist with optimistic updates

## Schema Notes
- trip_packing_items.notes field used to store "owner:<value>|category:<value>" serialization
  (schema has no dedicated owner text field; owner_user_id is for auth users only)

## Supabase Gate
Schema verified: trips (id, family_id, destination, start_date, end_date, notes) and trip_packing_items (id, trip_id, item, notes, packed, owner_user_id) exist per database.types.ts.
